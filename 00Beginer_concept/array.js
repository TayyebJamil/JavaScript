var arr=[];
 
 
  for (var i = 0; i < 3; i++) {
     arr.push(prompt(`Enter element ${i+1}: `)); //input array elements
 }

 
 
 console.log(arr[1]) //array length
//document.write(arr,"array length :",arr.length)
